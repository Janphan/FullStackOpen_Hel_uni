{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "build:ui": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && npm --prefix ../frontend install && npm --prefix ../frontend run build && node -e \"require('fs').cpSync('../frontend/dist','dist',{recursive:true})\"",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs"
  },
  "author": "Hong",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.21.2",
    "mongodb": "^7.1.0",
    "mongoose": "^9.3.1"
  },
  "devDependencies": {
    "@eslint/js": "^8.57.1",
    "@stylistic/eslint-plugin": "^3.1.0",
    "eslint": "^8.57.1",
    "nodemon": "^3.1.9"
  }
}
